{
  "name": "apdocentepba-v2",
  "private": true,
  "version": "0.1.0",
  "description": "APDocentePBA frontend + Cloudflare Worker source control",
  "scripts": {
    "dev:worker": "wrangler dev",
    "deploy:worker": "node -e \"console.error('PRODUCTION_SOURCE_GUARD_V1: direct repository deploy disabled; use verified live-bundle deployment workflow'); process.exit(1)\"",
    "tail:worker": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.20.3"
  }
}
